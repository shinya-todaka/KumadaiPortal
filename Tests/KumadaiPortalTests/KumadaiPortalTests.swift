import XCTest
@testable import KumadaiPortal

final class KumadaiPortalTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(KumadaiPortal().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
